# Net-Scanify
**Net Scanify** is a Python-based tool that scans devices connected to your local network using ARP requests.   Built with `scapy`, `tkinter`, and `pyfiglet`, it displays the IP, MAC address, and hostname of each device.   The tool is fully cross-platform and works on **Windows**, **Linux (including Kali)** with minimal setup.
